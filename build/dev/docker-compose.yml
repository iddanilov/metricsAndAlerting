version: "2.2"

services:
  postgres:
    image: postgres:10.14
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
    ports:
      - "6432:5432"